TODO:-
	Remove precedence storage, use 65816 stack (?)
	Indirection binary Functions ? ! $
	Variable handler.
	Assignment
	Assignment Indirection Protection to a?b a!b a$b (can be disabled)

Outstanding:
	Make ROMmable by copying code into RAM and running it from there.
	Expand Expression stack from little test one.
	Check Expression complexity (e.g. stack depth)
	# operator in tokenising/detokenising constant does it in hex.
	Signed modulus issue ?

